extends includes/layout.pug

block vars
  - var title = "Dump | Static Suite Data Server"

block content
  h1(class="title is-1")="Dump"
  table(class="table is-fullwidth is-hoverable")
    tbody
      each description, link in links
        tr
          td
            a(href=link)= link
          td=description
